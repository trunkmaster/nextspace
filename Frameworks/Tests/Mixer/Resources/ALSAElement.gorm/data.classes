{
    "## Comment" = "Do NOT change this file, Gorm maintains it";
    ALSAElement = {
	Actions = (
	    "setMute:",
	    "setBalance:",
	    "setVolume:"
	);
	Outlets = (
	    box,
	    volumeField,
	    volumeSlider,
	    balanceSlider,
	    muteButton,
	    window,
	    volumeRight,
	    volumeLeft
	);
	Super = NSObject;
    };
    FirstResponder = {
	Actions = (
	    "setMute:",
	    "setBalance:",
	    "setVolume:"
	);
	Super = NSObject;
    };
}