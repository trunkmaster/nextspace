{
    "## Comment" = "Do NOT change this file, Gorm maintains it";
    AppController = {
	Actions = (
	    "showPrefPanel:"
	);
	Outlets = (
	);
	Super = NSObject;
    };
    FirstResponder = {
	Actions = (
	    "browserClick:",
	    "updateOutputProfileList:",
	    "orderFrontFontPanel:",
	    "showPrefPanel:"
	);
	Super = NSObject;
    };
    PulseAudio = {
	Actions = (
	    "browserClick:",
	    "updateOutputProfileList:"
	);
	Outlets = (
	    devicesBrowser,
	    streamsBrowser,
	    window,
	    cardInfo,
	    deviceMute,
	    serverInfo,
	    streamMute,
	    streamVolume,
	    appBrowser,
	    appMute,
	    appVolume,
	    modeButton,
	    outputBalance,
	    outputDevice,
	    outputDeviceProfile,
	    outputMute,
	    outputVolume
	);
	Super = NSObject;
    };
}