{
    "## Comment" = "Do NOT change this file, Gorm maintains it";
    AppController = {
	Actions = (
	    "showPrefPanel:"
	);
	Outlets = (
	);
	Super = NSObject;
    };
    FirstResponder = {
	Actions = (
	    "appMuteClick:",
	    "browserClick:",
	    "outputSetBalance:",
	    "orderFrontFontPanel:",
	    "outputMute:",
	    "outputSetVolume:",
	    "showPrefPanel:",
	    "updateOutputProfileList:"
	);
	Super = NSObject;
    };
    Mixer = {
	Actions = (
	    "browserClick:",
	    "updateOutputProfileList:",
	    "appMuteClick:",
	    "outputSetBalance:",
	    "outputMute:",
	    "outputSetVolume:"
	);
	Outlets = (
	    devicesBrowser,
	    streamsBrowser,
	    window,
	    cardInfo,
	    deviceMute,
	    serverInfo,
	    streamMute,
	    streamVolume,
	    appBrowser,
	    appMute,
	    appVolume,
	    modeButton,
	    outputBalance,
	    outputDevice,
	    outputDeviceProfile,
	    outputMute,
	    outputVolume
	);
	Super = NSObject;
    };
}