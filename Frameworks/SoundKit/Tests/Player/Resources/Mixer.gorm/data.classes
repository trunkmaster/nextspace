{
    "## Comment" = "Do NOT change this file, Gorm maintains it";
    AppController = {
	Actions = (
	    "showPrefPanel:"
	);
	Outlets = (
	);
	Super = NSObject;
    };
    FirstResponder = {
	Actions = (
	    "appMuteClick:",
	    "browserClick:",
	    "setDeviceCard:",
	    "orderFrontFontPanel:",
	    "setDeviceBalance:",
	    "setDeviceMute:",
	    "setDevicePort:",
	    "setDeviceProfile:",
	    "setDeviceVolume:",
	    "setMode:",
	    "showPrefPanel:"
	);
	Super = NSObject;
    };
    Mixer = {
	Actions = (
	    "browserClick:",
	    "setDevicePort:",
	    "appMuteClick:",
	    "setDeviceBalance:",
	    "setDeviceMute:",
	    "setDeviceVolume:",
	    "setDeviceProfile:",
	    "setMode:",
	    "setDeviceCard:"
	);
	Outlets = (
	    window,
	    appBrowser,
	    appMute,
	    appVolume,
	    modeButton,
	    deviceBalance,
	    deviceMuteBtn,
	    devicePortBtn,
	    deviceProfileBtn,
	    deviceVolumeSlider,
	    deviceBalanceSlider,
	    deviceBox,
	    deviceCardBtn
	);
	Super = NSObject;
    };
}