{
    "## Comment" = "Do NOT change this file, Gorm maintains it";
    AppController = {
	Actions = (
	    "showPrefPanel:"
	);
	Outlets = (
	);
	Super = NSObject;
    };
    FirstResponder = {
	Actions = (
	    "appMuteClick:",
	    "browserClick:",
	    "setMode:",
	    "orderFrontFontPanel:",
	    "outputMute:",
	    "outputSetBalance:",
	    "outputSetVolume:",
	    "setOutputPort:",
	    "setOutputProfile:",
	    "showPrefPanel:"
	);
	Super = NSObject;
    };
    Mixer = {
	Actions = (
	    "browserClick:",
	    "setOutputPort:",
	    "appMuteClick:",
	    "outputSetBalance:",
	    "outputMute:",
	    "outputSetVolume:",
	    "setOutputProfile:",
	    "setMode:"
	);
	Outlets = (
	    window,
	    cardInfo,
	    deviceMute,
	    appBrowser,
	    appMute,
	    appVolume,
	    modeButton,
	    deviceBalance,
	    devicePort,
	    deviceProfile,
	    deviceVolume,
	    deviceBox
	);
	Super = NSObject;
    };
}