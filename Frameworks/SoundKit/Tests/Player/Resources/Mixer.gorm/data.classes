{
    "## Comment" = "Do NOT change this file, Gorm maintains it";
    AppController = {
	Actions = (
	    "showPrefPanel:"
	);
	Outlets = (
	);
	Super = NSObject;
    };
    FirstResponder = {
	Actions = (
	    "appMuteClick:",
	    "browserClick:",
	    "setMode:",
	    "orderFrontFontPanel:",
	    "setDeviceMute:",
	    "setDeviceBalance:",
	    "setDeviceVolume:",
	    "setDevicePort:",
	    "setDeviceProfile:",
	    "showPrefPanel:"
	);
	Super = NSObject;
    };
    Mixer = {
	Actions = (
	    "browserClick:",
	    "setDevicePort:",
	    "appMuteClick:",
	    "setDeviceBalance:",
	    "setDeviceMute:",
	    "setDeviceVolume:",
	    "setDeviceProfile:",
	    "setMode:"
	);
	Outlets = (
	    window,
	    cardInfo,
	    appBrowser,
	    appMute,
	    appVolume,
	    modeButton,
	    deviceBalance,
	    deviceMuteBtn,
	    devicePortBtn,
	    deviceProfileBtn,
	    deviceVolumeSlider,
	    deviceBalanceSlider,
	    deviceBox
	);
	Super = NSObject;
    };
}