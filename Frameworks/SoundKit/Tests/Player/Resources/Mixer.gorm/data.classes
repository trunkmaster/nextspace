{
    "## Comment" = "Do NOT change this file, Gorm maintains it";
    AppController = {
	Actions = (
	    "showPrefPanel:"
	);
	Outlets = (
	);
	Super = NSObject;
    };
    FirstResponder = {
	Actions = (
	    "appMuteClick:",
	    "browserClick:",
	    "setOutputProfile:",
	    "orderFrontFontPanel:",
	    "outputMute:",
	    "outputSetBalance:",
	    "outputSetVolume:",
	    "setOutputPort:",
	    "showPrefPanel:"
	);
	Super = NSObject;
    };
    Mixer = {
	Actions = (
	    "browserClick:",
	    "setOutputPort:",
	    "appMuteClick:",
	    "outputSetBalance:",
	    "outputMute:",
	    "outputSetVolume:",
	    "setOutputProfile:"
	);
	Outlets = (
	    devicesBrowser,
	    streamsBrowser,
	    window,
	    cardInfo,
	    deviceMute,
	    serverInfo,
	    streamMute,
	    streamVolume,
	    appBrowser,
	    appMute,
	    appVolume,
	    modeButton,
	    outputBalance,
	    outputDevice,
	    outputDeviceProfile,
	    outputMute,
	    outputVolume
	);
	Super = NSObject;
    };
}