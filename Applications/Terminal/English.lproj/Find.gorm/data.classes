{
    "## Comment" = "Do NOT change this file, Gorm maintains it";
    TerminalFinder = {
	Actions = (
	);
	Outlets = (
	    findNextButton,
	    findPanel,
	    findTextField,
	    ignoreCaseButton
	);
	Super = NSObject;
    };
}