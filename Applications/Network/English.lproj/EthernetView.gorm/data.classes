{
    "## Comment" = "Do NOT change this file, Gorm maintains it";
    EthernetController = {
	Actions = (
	);
	Outlets = (
	    configureMethod,
	    defaultGateway,
	    dnsServers,
	    ipAddress,
	    ethernetView,
	    searchDomains,
	    subnetMask
	);
	Super = NSObject;
    };
    FirstResponder = {
	Actions = (
	    "connectionListClick:"
	);
	Super = NSObject;
    };
}